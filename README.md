# realFront

Lalita Real Estate ERP + CRM — **Frontend**

- React (Vite) · Ant Design · React Router

## Setup

```bash
npm install
npm run dev
```

App default: `http://localhost:5173`  
Configure `VITE_API_URL` in `.env` (e.g. `http://localhost:5000/api`).
